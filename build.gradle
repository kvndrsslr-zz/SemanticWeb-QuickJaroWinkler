apply plugin: 'java'

repositories {
    mavenCentral()
}
dependencies {
    //for dependencies found in artifact repositories you can use
    //the group:name:version notation
    compile files('libs/nxparser-1.2.3.jar')
    compile 'commons-cli:commons-cli:1.2'
}